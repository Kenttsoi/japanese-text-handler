[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "japanese-text-handler"
version = "0.1.0"
description = ""
requires-python = ">=3.10"
readme = "README.md"
authors = [{name = "", email = ""}]
license = {text = "MIT"}
dependencies = [
  "Flask==3.1.3",
  "python-dotenv>=1.0.0",
  "gunicorn",
]